import { StyleSheet } from "react-native";

export const DealbuttonStyle = StyleSheet.create({
  button: {
    backgroundColor: "#0C1B32",
    borderRadius: 30,
    alignItems: "center",
    justifyContent: "center",
    height: 40,
  },
  buttonText: {
    fontSize: 12,
    color: "#fff",
  },
});
